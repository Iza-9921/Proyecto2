package com.example.todoaccesible.data.local.entities

/** Un "tipo de inmueble" con su propio cuestionario (secciones/preguntas) independiente. */
data class TipoCuestionarioEntity(
    val nombre: String,
    val orden: Int
)
